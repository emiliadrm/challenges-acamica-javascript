// 1. Crea una función que tome una lista de nombres (un array), y un nombre (un string). 
// La función deberá RETORNAR un nuevo array con los mismos nombres del array
// junto al nuevo nombre insertado dentro del array. Por ejemplo,
// Supongamos que tenemos esta lista de personas:
/*
let followers = [
    "@wesbox",
    "@dan_abramov",
    "@stolinkski",
    "@ryanflorence",
    "@kentcdodds",
    "@sophiebits",
    "@acdlite",
    "@sebmarkbage",
    "@stevekinney"
  ];
  
  // Y queremos agregar un nuevo username: "@unUsername"
  // Crea tu función debajo de este comentario.
  
  function agregarNombre(followers, user) {
      followers.push(user);
      return followers;
}
  
  // E invocala de esta forma:
  let nuevosNombres = agregarNombre(followers, "@unNuevoNombre");
  nuevosNombres = agregarNombre(followers, "@emilia");
  
  // En la línea anterior estamos creando una variable nueva la cual será igual a lo que sea que la función agregarNombre retorne.
  // la función agregarNombre esta tomando como argumentos el array followers, declarado en la línea 6, y un string con un nomre
  // de usuario.
  
  // Descomenta la siguiente línea para verificar que el nuevo nombre fue agregado.
console.log(nuevosNombres);
console.log(followers) */